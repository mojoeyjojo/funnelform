import Generator from "@/components/Generator";

// Phase 1 bare UI: paste a URL -> watch the real pipeline -> see the generated,
// schema-valid quiz rendered, with the Love it / Not quite rating and the
// builder_events recorded.
export default function Home() {
  return <Generator />;
}
